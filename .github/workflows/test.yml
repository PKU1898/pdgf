name: CI Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: pdgf
          POSTGRES_PASSWORD: pdgf123
          POSTGRES_DB: pdgf_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U pdgf"
          --health-interval=5s
          --health-timeout=3s
          --health-retries=5

    env:
      DATABASE_URL_TEST: postgresql://pdgf:pdgf123@localhost:5432/pdgf_test?schema=public
      JWT_SECRET: test-jwt-secret-for-ci

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Cache pnpm store (server)
        uses: actions/cache@v4
        with:
          path: server/node_modules
          key: ${{ runner.os }}-server-${{ hashFiles('server/pnpm-lock.yaml') }}

      - name: Cache pnpm store (miniapp)
        uses: actions/cache@v4
        with:
          path: miniapp/node_modules
          key: ${{ runner.os }}-miniapp-${{ hashFiles('miniapp/pnpm-lock.yaml') }}

      - name: Install server dependencies
        working-directory: server
        run: pnpm install --frozen-lockfile

      - name: Generate Prisma client
        working-directory: server
        run: pnpm db:generate

      - name: Run database migrations
        working-directory: server
        run: pnpm exec prisma migrate deploy

      - name: Run server tests
        working-directory: server
        run: pnpm test

      - name: Install miniapp dependencies
        working-directory: miniapp
        run: pnpm install --frozen-lockfile

      - name: Run miniapp tests
        working-directory: miniapp
        run: pnpm test
